# 커맨드 플레이북 (Command Playbook)

사용자는 명령으로만 작업한다. Claude는 명령이 오면 **먼저 이 표에서 해당 행을 찾아** 절차·판단지점·건드릴 파일을 확인하고 실행한다. 없는 명령이면 가장 가까운 행을 기준으로 처리하고, 새로 생긴 반복 명령은 이 파일에 행을 추가한다.

## 보고 규약 (매 명령 응답)

1. 바뀐 것 1~3줄
2. 판단 필요 시 옵션 A/B (권장안 먼저), 그 외엔 결정해서 진행하고 한 줄로 밝힘
3. 빌드 결과: `경고 N / 오류 N` (빌드법은 `docs/engine-overview.md` "빌드")
4. 커밋은 명시 요청 시에만

## 명령 → 처리 표

| # | 트리거 예시 | Claude가 하는 일 | 사용자가 판단할 것 | 건드리는 파일 |
|---|---|---|---|---|
| 1 | "위젯 하나 추가해 (체크박스/슬라이더/…)" | **체크박스·슬라이더 있음**(`ui::CheckBox`/`ui::Slider`, `docs/ui-architecture.md` "각 위젯의 책임"). 새 위젯은 `ui::Widget` 상속: `Build`(로컬좌표→`Quad`), 필요 시 `PointerMove/Down/Up`(소비 시 `true`). 화면에 꽂는 건 `UIContext` 생성자가 아니라 `game/<Screen>.cpp`의 화면 빌더 함수(`docs/scene-flow-design.md`) | 위젯 종류·기본 크기·색. 상호작용 콜백 시그니처 | `src/ui/UI.h`, `src/ui/UI.cpp` |
| 1b | "긴 목록 / 스크롤되는 리스트 / 인벤토리 UI" | **`ui::ScrollList` 있음 (v1, `docs/scrollable-list-and-pool.md`)** — 가상화(뷰포트+overscan 만큼만 행, 스크롤 시 재바인딩), `ListModel`(DIP) + `RowView`(ISP), 선택/hover 는 데이터 인덱스로 보관, 클리핑 A(내부 `ClampQuad`, 렌더러 무변경), 고정 행 높이. 휠: `Win32Window WM_MOUSEWHEEL` → `IWindowEventSink::OnMouseWheel` → `InputState` 누적 → `UIContext::PointerWheel` → `Widget::PointerWheel`. 데모 `game/InventoryScreen`(타이틀 ITEMS). **미구현**: 범용 `core::ObjectPool<T>`, 가변 행 높이, 키보드 네비, 클리핑 B(scissor). 새 목록 = `ListModel` 구현 + `BuildXxxScreen` 자유 함수 | 데모 대상, 항목 소스(span vs 소유), 클리핑 B 로 올릴지(텍스처 아이템이면) | `src/ui/ScrollList.*`, `src/game/<X>Screen.*`, (휠 확장 시) `src/platform/Win32Window.*`·`src/input/InputState.h` |
| 2 | "게임 시스템 추가해 (물리/충돌/애니메이션/스폰)" | `src/game/`에 클래스. `Simulation`이 소유하거나 `Application` 스텝 루프에서 호출. 병렬 필요 시 `JobSystem::ParallelFor`(겹치지 않는 `[begin,end)`), `Fence`는 단계 경계에서만 | 고정 timestep에서 돌지 / 프레임당 1회인지. 데이터 레이아웃 AoS vs SoA | `src/game/Simulation.*` 또는 새 `src/game/<System>.*`, `src/game/Application.*` |
| 2c | "충돌 붙여 / 콜라이더 추가해" | **이미 있음**: `physics/p2d`·`physics/p3d`에 Box/Circle·Box/Sphere + `CollisionWorld*`(N² 탐지). `Simulation`이 소유·매 스텝 구동. **레이캐스트 3D 됨**(`Ray3D`/`RayHit3D` + `CollisionWorld3D::{RaycastClosest,RaycastAny,RaycastAll}` 선형 스캔, `docs/collider-design.md` "레이캐스트"). 확장은 브로드페이즈·레이캐스트 2D·`Simulation` 연동·트리거 enter/exit·응답 | 브로드페이즈(그리드/SAP), 트리거 이벤트 vs "지금 겹침", 응답을 physics에 넣을지 게임에 둘지 | `src/physics/*`, `src/game/Simulation.*` |
| 2e | "디버그 라인/AABB/레이 그려줘" | **됨**: `render/r3d/DebugDrawPass` + `Scene3D::debugLines`(`DebugLine{a,b,color}`) + 헤더 헬퍼 `render::debug::{Line,Box,Sphere,Ray}`(`Scene3D.h`) + `debugline.hlsl`(월드 라인리스트, depth-test/no-write). `SnapshotBuilder` 에서 값으로 방출. 2D 디버그 드로우는 아직 | 색/수명, 스크린공간 2D 버전, 릴리스 빌드 포함 여부 | `src/render/r3d/DebugDrawPass.*`, `assets/shaders/debugline.hlsl`, `src/render/r3d/Scene3D.h`, `src/game/SnapshotBuilder.cpp` |
| 2f | "수천 개체 출력 / 인스턴싱 / 대규모 오브젝트 렌더" | **설계만 (`docs/instanced-rendering.md`)** — 강체 인스턴스(트랜스폼만 다른 같은 메시)를 배치당 `DrawIndexedInstanced` 1콜로. `render/r3d/Scene3D.h` 에 `MeshInstance`(20B POD: pos/yaw/scale/colorRgba) + `InstanceBatch`(meshId·연속구간·lod) 추가, `MeshPass3D` 를 확장(slot1 `PER_INSTANCE_DATA` + `mesh_instanced.hlsl` + DYNAMIC 인스턴스 VB Map). 컬링·LOD 는 `SnapshotBuilder`(메인 스레드)에서 — 프러스텀(`math::MakeFrustum`) + 거리 버킷. 심 쪽 선행 = `core::ObjectPool<T>` + `game/AgentStore`(SoA, free-list, 스텝 사이 스폰). `meshDraws` 는 유니크 프롭용으로 유지(OCP). 애니메이션 군중은 2d/§5(VAT). 구현 순서·사용법은 그 문서 §8·§9 | 트랜스폼 포맷(압축 20B vs 4x3 48B), 색 per-instance vs per-batch, `MeshPass3D` 확장 vs 새 패스, 규모 목표(동시 N@60fps), SoA 전환 시점 | `src/render/r3d/Scene3D.h`, `src/render/r3d/MeshPass3D.*`·`assets/shaders/mesh_instanced.hlsl`, `src/game/SnapshotBuilder.cpp`, `src/math/Math3D.h`(프러스텀), 새 `src/core/ObjectPool.h`·`src/game/AgentStore.*` |
| 2d | "대규모 좀비 웨이브 / 호드 / 군중 (Back4Blood 식)" | **설계만 (`docs/horde-design.md`)** — 플로우필드(전역 경로, 그리드 1장) + SoA `game/Horde`(`JobSystem::ParallelFor` 스티어링, 분리력·밀도 climb) + 새 `render/r3d/HordePass3D`(VAT GPU 스킨 + `DrawIndexedInstanced` 1콜, LOD/임포스터). **`ModelDraw` 경로로 만들지 말 것** — CPU 스킨 1마리 상한. 스냅샷엔 20B `HordeInstance` 배열만. 충돌은 physics 브로드페이즈(2c) 와 합류, 좀비끼리는 분리력이 대신. 구현 순서·사용법은 그 문서 §7·§8 | 규모 목표(동시 N마리 @60fps), VAT 로드 시 vs 오프라인 베이크, climb 진짜 +Y vs 가짜, 플로우필드로 충분한지(다층/동적 장애물) | 새 `src/game/{Horde,FlowField,WaveDirector}.*`, 새 `src/render/r3d/HordePass3D.*`·`assets/shaders/horde.hlsl`, `src/render/r3d/Scene3D.h`, `src/game/SnapshotBuilder.*`, `src/main.cpp`, `src/physics/p3d/*`(브로드페이즈) |
| 3 | "스프라이트(텍스처) 그릴 수 있게 해 / 이미지 아틀라스 / 폰트 아틀라스" | **부분 구현** — 런타임(`render::SpriteDraw` + `render/r2d/SpritePass2D`(그룹핑·scissor) + `sprite2d.hlsl` + `render/r2d/TextureAtlas`(`.atlas` 파서 + DX10 `.dds` 로더) + `SnapshotBuilder` 데모 방출) + **오프라인 패커 `tools/atlas_pack` v1**(플랫 `assets/atlas/atlas.groups` → shelf 패킹 + gutter + box 밉 + 무압축 `_SRGB` `.dds`/`.atlas`/`.cache`; 디코드는 `import::LoadImageFromFile` 재사용; 빌드 `tools/build_atlas_pack.bat`) 됨. **미구현**: `ui::DrawList`/클립 스택, `ScrollList` 연동, `GlyphAtlas`(폰트), **BC7/BC4 압축**(`bc7enc` vendor + `--format bc7`), `AssetRegistry`/`loading-and-streaming` 연동. 상세 `docs/texture-atlas-and-sprite-pass.md` §7, `docs/atlas-build-pipeline.md`, `docs/image-assets.md`. `render::SpriteDraw`(dest + uv + tint + atlasId + `clip` scissor rect) + `render/r2d/SpritePass2D`(`(atlasId, clip)` 그룹당 draw 1, `RSSetScissorRects`) + `sprite2d.hlsl` + `render::TextureAtlas`/`AtlasIndex` + `GlyphAtlas`(폰트도 아틀라스). UI 는 `ui::DrawList`(SpriteDraw 버퍼 + `PushClip`/`PopClip`)로, 단색 = 흰 스프라이트. 아틀라스는 **그룹 번들**(`ui`/`char/<name>`/`obj/<cat>`, 그룹별 증분 빌드) → `.dds` BC7/BC4 고정, 페이지 `{1024,2048,4096}`, `loading-and-streaming` `AssetKind::Atlas`. **DirectX 데스크톱 전용 — 모바일 미고려.** `tools/atlas_pack`(오프라인, 엔진 밖) | 이번 범위(문서/구현), UI 프리미티브 통일(흰 스프라이트) vs Quad+Sprite 분리, `QuadPass2D` 은퇴 시점 | 새 `src/render/r2d/SpritePass2D.*`·`assets/shaders/sprite2d.hlsl`, `src/render/r2d/Sprite2D.h`, `src/render/RenderSnapshot.h`, `src/render/Dx11Renderer.*`(scissor state), `src/ui/*`, `src/main.cpp`, 새 `tools/atlas_pack.*` |
| 3b | "렌더 패스/스테이지 추가해 (그림자·블룸·디버그 라인·포스트프로세스)" | `render::IRenderPass` 구현. 셰이더는 `assets/shaders/<name>.hlsl` + `Initialize(device, ShaderLibrary&)` 에서 `shaders.Get(...)`. `main.cpp` 에서 `AddRenderPass`. 데이터는 스냅샷 값 타입 | 패스 순서, 자체 RT 필요 여부, 스냅샷에 뭘 실을지 | 새 `src/render/r2d/` 또는 `r3d/`, `assets/shaders/*.hlsl`, `src/main.cpp`, 스냅샷 헤더, vcxproj (셰이더는 `<None>`) |
| 3f | "셰이더 시스템 개선 (디파인/순열, .cso 캐시, 컴퓨트)" | `ShaderLibrary::Get` 에 `D3D_SHADER_MACRO*` 인자, 바이트코드 디스크 캐시, `CSMain`/`cs_5_0` 지원. 설계는 `docs/shader-pipeline.md` "다음" | 순열 키 방식, 캐시 무효화, Release 빌드 리로드 정책 | `src/render/shader/ShaderLibrary.*` |
| 3g | "툰 룩 조정 (셀 밴드/아웃라인/크리즈)" | **있음**: 셀(`common3d.hlsli::ApplyCelLighting`), 인버티드 헐 아웃라인(`outline.hlsl` + `kOutlineWidth`), 크리즈 라인(`import/CreaseLines` + `CreaseOptions`). 세부·튜닝표는 `docs/toon-rendering.md` §7 | 아웃라인 방식(헐/포스트/림), 크리즈 임계각, 화면공간 vs 월드공간 폭 | `assets/shaders/*.hlsl`, `src/render/r3d/ModelMeshPass3D.*`, `src/import/CreaseLines.*` |
| 3i | "AA 개선 (셀 밴드/컷아웃/포스트)" | **MSAA 있음** (씬 타깃 최대 8x + resolve, `docs/msaa.md`). 다음: alpha-to-coverage(컷아웃 엣지), `ApplyCelLighting` 밴드에 `smoothstep`, 포스트 FXAA(오프스크린 RT 선행). 샘플 수는 `Dx11Renderer::CreateDeviceAndSwapChain` 후보 리스트 | 셀 단차를 셰이더 vs 포스트로, 8x 비용, 샘플 수를 FrameSettings 로 노출할지 | `src/render/Dx11Renderer.*`, `assets/shaders/common3d.hlsli`, 새 포스트 패스 |
| 3h | "조명 추가/개선 (포인트·스팟·그림자·rim)" | **key + ambient 있음** (`render/r3d/Lighting.h`, `Scene3D::lighting`). **그림자 있음**(단일 방향 셰도우맵 + PCF, `docs/shadows.md`). 다음: 라이트 배열(cbuffer/StructuredBuffer), CSM/카메라 추종, rim. 설계는 `docs/lighting.md` 로드맵 | 라이트 개수 상한·컬링, 그림자 해상도, `BuildLighting` 을 게임 상태 기반으로 | `src/render/r3d/Lighting.h`, `FrameConstants.h`, `assets/shaders/common3d.hlsli`, `src/game/SnapshotBuilder.cpp` |
| 3c | "FBX 모델 그려" | **됨(정적 + 텍스처)**: `ModelMeshPass3D` 가 시작 시 FBX + 디퓨즈 TGA 로드 → 바인드 포즈 렌더. 남은 일은 3e | | `src/render/r3d/ModelMeshPass3D.*` |
| 3d | "텍스처 개선 (노멀맵/스페큘러/밉맵/PNG·DDS)" | **PNG/JPG/BMP/GIF 로더 있음** — `stb_image` vendored(`src/vendor/stb`, MIT/PD v2.30), 진입점 `import::LoadImageFromFile(path)` → `import::ImageData`(RGBA8 top-down), `.tga` 는 자체 `LoadTga`. 규칙(색공간·알파·dev/ship)은 `docs/image-assets.md`. `ModelMeshPass3D` 디퓨즈 SRV = `_UNORM_SRGB`. 남은 것: `_NRM`/`_SPEC` 슬롯(→ `_UNORM` linear) + 셰이더 확장, `MISC_GENERATE_MIPS` 밉, 탄젠트(importer) | 노멀맵 공간, 반투명 머티리얼 정렬, 새 포맷 = `STBI_ONLY_*` + `ImageFile.cpp` 디스패치 | `src/import/ImageFile.*`, `src/import/ImageData.*`, `src/vendor/stb/*`, `src/render/r3d/ModelMeshPass3D.*` |
| 3e | "캐릭터 애니메이션 (스키닝)" | **됨 (CPU 스키닝)**: `ModelMeshPass3D` 가 `animClipIndex`/`animClipTime`(Scene3D::ModelDraw)를 보고 매 프레임 `AnimationSampler::Evaluate` + LBS 로 정점 재계산해 DYNAMIC VB 업로드(셰이더 무변경). 클립은 `import::LoadAnimationClipsFromFile` 로 별도 FBX에서 본 이름 매칭 리타깃. Unity-chan 26개 클립은 `render/r3d/CharacterAnimationClips.h`. 재생 상태는 `game::CharacterAnimationState`(`enum Locomotion{Wait,Walk,Run,Jump}` → 클립 스냅), `Simulation::StepOneActor`(플레이어 = `m_actors[0]`) 가 게임플레이 신호로 구동 — 데모 씬 전체는 `docs/demo-scene.md`. **재생 모드(`anim::PlayMode{Loop,Once,PingPong}`) + 0.15s 크로스페이드(`AnimationSampler::EvaluateBlended`, 로컬 TRS lerp) + 파라메트릭 점프(`UpdateParametric`, phase 0..1) 됨** (`docs/roadmap.md` §2.1). 여러 인스턴스를 각자 다른 애니메이션으로 세우려면 여전히 §5.2 GPU `SkinnedMeshPass3D` 필요. 상세 `docs/model-animation-research.md` §5.2a/§5.3a, 전체 지도는 `docs/animation-design.md` §1 | (해결됨) 다음 후보는 GPU 스킨 전환·블렌드 트리·루트 모션 | `src/render/r3d/ModelMeshPass3D.*`, `src/render/r3d/CharacterAnimationClips.h`, `src/import/ModelImporter.*`, `src/game/CharacterAnimationState.*`, `src/game/Simulation.*` |
| 3ea | "2D 스프라이트 애니메이션 (프레임 애니메이션)" | 설계됨(미구현): `anim::a2d::SpriteAnimationClip`/`SpriteAnimator` — uv rect 프레임 배열을 시간에 따라 스텝. `SnapshotBuilder`가 `CurrentFrame().uvRect`를 `SpriteDraw`에 복사. 전제: 텍스처 `SpriteDraw`(#3) 선행 필요. 설계 `docs/animation-design.md` §2 | 클립 데이터 포맷(코드 배열 vs 에셋 파일), 상태 머신(`anim::core`) 도입 시점 | 새 `src/anim/a2d/*`, `src/render/r2d/Sprite2D.h`, `src/game/SnapshotBuilder.*` |
| 3eb | "Live2D/Spine 같은 2D 리깅 애니메이션" | **연구 선행 필요** — 바로 설계·구현하지 않는다. `docs/animation-design.md` §4 의 조사 항목(라이선스, 포맷, vendor 가능 여부) 먼저 별도 조사 문서로 정리 | 상용 SDK 라이선스 수용 여부, 아트 파이프라인(실제 사용 툴) 확인이 먼저 | `docs/animation-design.md` (조사 진행 시 새 `docs/2d-mesh-animation-research.md`) |
| 4 | "실제 게임 시작하자 / 씬·엔티티 만들어" | **씬/스테이트 됨**: `GameState{Title,InGame}` + Settings 모달 오버레이, `docs/scene-flow-design.md`. **엔티티 식별 뼈대 됨**: `core::EntityId`/`core::EntityRegistry`(식별·생존주기만, 컴포넌트 데이터는 각 시스템이 자기 방식대로 — 단순 struct 배열 또는 컴포넌트 테이블, 둘 다 지원) — `docs/entity-lifecycle-design.md`, 아직 `Simulation`엔 미연결(YAGNI, 실제 엔티티 종류 나올 때 연결). 남은 일: `Simulation`의 데모 페이로드(파티클+회전 큐브)를 실제 콘텐츠로 교체. `SnapshotBuilder`가 엔티티 → `MeshDraw`/`Quad`. 장르·시놉시스는 `docs/synopsis.md`(초안, 미확정) | 장르·핵심 루프(2D인지 3D인지) — `docs/synopsis.md` 확정 필요. 엔티티마다 단순 struct 배열 vs 컴포넌트 테이블 중 뭘 쓸지(`entity-lifecycle-design.md` §3). 데모 페이로드 유지 여부 | `src/game/Simulation.*`, `src/game/SnapshotBuilder.*`, `src/core/EntityId.h`, `src/core/EntityRegistry.*`, 새 `src/game/*` |
| 4a | "설정 화면 / 타이틀 화면 열어줘" | **됨**: Title→InGame 전환, Settings 오버레이(사운드·마우스·해상도·vsync). `docs/scene-flow-design.md`(씬), `docs/game-settings.md`(설정 카탈로그) | 새 설정 항목 추가 시 실제 적용까지 할지, 저장만 할지(§1 표) | `src/game/Application.*`, `src/game/TitleScreen.*`, `src/game/InGameHud.*`, `src/game/SettingsScreen.*`, `src/core/Settings.*` |
| 4b | "3D 카메라 조작 붙여 (WASD·마우스 등)" | **됨 (3인칭 오빗 팔로우 + 마우스 잠금)**: `Simulation` 이 `m_cameraYaw/Pitch` + `m_actors[0]`(플레이어) 보유, `UpdateCameraLook`(프레임당 1회) + `StepActors`/`StepOneActor`(고정 스텝). `Application::BuildPlayerIntent` 가 WASD/Shift/Space/MouseDelta → `PlayerIntent`. `Win32Window::SetPointerLocked` 가 raw input + 커서 잠금. `SnapshotBuilder::BuildCamera` 가 그 상태로 view. 데모 씬은 `Simulation::kDemoScene`(1=로컬배속 액터 3인, 2=절벽 조망 + `SimAgent` 군중). 상세·튜닝은 `docs/demo-scene.md` | FPS / 오빗 / 고정 (현재 오빗). 감도·역전은 `Simulation` 상수 (설정 UI 연결은 안 함) | `src/game/Simulation.*`, `src/game/Application.*`, `src/game/SnapshotBuilder.cpp`, `src/platform/Win32Window.*`, `src/input/InputState.h` |
| 4d | "시간 배속 / 슬로모 / 일시정지 / 특정 개체만 가속" | **됨 (`docs/time-design.md` "시간 배율")**. 전역: `Application::SetGlobalTimeScale` → `m_timestep.Advance(delta * scale)` (스텝 크기 불변, 결정성 유지). `scale 0` = 일시정지, 이때도 `ignoreGlobalPause` 액터는 `Step(..., globalPaused=true)` 로 진행. 개체별: `Simulation::Actor{ timeScale, ignoreGlobalPause }`, `StepActors` 가 `timeScale>1` 을 서브스텝(`kMaxActorSubSteps=8`)으로 적분. 데모 = `SpawnActors` 의 액터 3개(1× / 3× / 0.35×), PageUp/PageDown 로 전역 배율 순환 | unscaled 클럭 분리(UI 애니가 pause 에 안 물리게) 언제 할지, 액터 간 상호작용 순서, 로컬 배율 부모/자식 합성 | `src/game/Application.*`, `src/game/Simulation.*`, `src/game/SnapshotBuilder.cpp`, `src/game/CharacterAnimationState.h` |
| 4e | "소리 / 효과음 / BGM / 오디오" | **최소 구현 (`docs/audio-design.md`)**: `engine::audio::AudioEngine`(pImpl, XAudio2) — 마스터 + music/sfx 서브믹스, `PlaySfx`(PCM WAV 원샷, voice-per-play + `OnBufferEnd` 회수), `PlayMusic`/`StopMusic`(루핑, 인메모리), `Update`(프레임당 1회). `Application` 만 호출(레이어 분리), 설정 볼륨 3개 → `ApplyVolumes`. XAudio2 실패 시 조용히 무동작. 데모 = Space 점프 시 `blip.wav`. **미구현**: 청크 스트리밍, `AssetKind::Audio` 통합, OGG, `X3DAudio`, voice 풀 | 스트리밍 언제, 압축 포맷(OGG vs 무압축), 3D 위치음 도입 시점, 게임플레이 이벤트를 `Application` 이 감지 vs `Simulation` 이 값으로 통지 | `src/audio/AudioEngine.*`, `src/game/Application.*`, `src/game/SettingsScreen.*`, `assets/audio/*` |
| 4c | "로딩 화면 / 씬 프리로드 / 스트리밍 / 위치 전환" | **설계만 (`docs/loading-and-streaming.md`)**. `GameState::Loading` + 커튼(진행바·로테이션 팁) + 전용 IO 스레드(`core::AssetLoader`, CPU 디코드만) + 렌더 스레드 시간분할 GPU 업로드 펌프 + `core::AssetRegistry`(ref-count·LRU) + `game::SceneLoader`(매니페스트 diff — 공유 에셋 재로드 0) + 인게임 무커튼 프리스트림(더블버퍼 `World` + 크로스페이드). 지금 `ModelMeshPass3D::Initialize` 의 동기 로드를 여기로 이관하는 게 마지막 마이그레이션 | 이번 범위(문서만/스캐폴드/실구현), 전환마다 커튼 vs 인접구역 프리스트림, IO 스레드 vs `JobSystem`, 메모리 예산·LRU 상한, 매니페스트를 코드 테이블 vs 파일 | 새 `src/core/AssetLoader.*`·`AssetRegistry.*`, `src/game/{SceneLoader,SceneManifests,SceneWorlds,LoadingScreen}.*`, `src/game/Application.*`, `src/game/Simulation.*`, `src/render/IRenderer.h` |
| 5 | "게임패드/다른 입력 지원해" | `input::InputState`에 상태+질의 추가. `platform`에 소스(XInput 등). `Application::BuildPlayerIntent`가 통합 | 지원 장치(XInput? RawInput?). 데드존·매핑 노출 위치 | `src/input/InputState.h`, `src/platform/*`, `src/game/Application.*` |
| 6 | "렌더 백엔드 바꿔/추가해 (DX12 등)" | 새 클래스가 `render::IRenderer` 구현. `main.cpp`에서 생성 타입만 교체. UI/게임 코드는 불변 | 교체 vs 런타임 선택. 스냅샷 포맷 변경 필요 여부 | 새 `src/render/<Backend>.*`, `src/main.cpp`, (필요 시) `src/render/RenderSnapshot.h` |
| 7 | "스왑체인 FLIP_DISCARD로" | `DXGI_SWAP_EFFECT_FLIP_DISCARD` + 프레임마다 RTV 재바인딩 확인. 필요 시 `IDXGISwapChain` 최소 버전 상향 | 없음 (권장 기본값). Win10 미만 지원 포기 여부 | `src/render/Dx11Renderer.cpp` |
| 8 | "시뮬/렌더 파이프라이닝 / 더블 버퍼링" | 스냅샷 더블버퍼 + 프레임 N 시뮬과 N-1 렌더 병렬. `Application` 루프 재구성 | 입력 지연 1프레임 허용치. 스냅샷 보간 여부 | `src/game/Application.*`, `src/render/Dx11Renderer.*` |
| 9 | "frame-time HUD / 프로파일러" | `core`에 스코프 타이머. `Application`이 프레임 시간·Fence 대기·worker 편차 수집 → UI `TextLine`으로 표시 | 표시 항목. 릴리스 빌드 포함 여부 | 새 `src/core/Profiler.*`, `src/game/Application.*`, `src/ui/UI.*` |
| 10 | "빌드해 / 실행해 / 스모크 테스트" | CLI 빌드(`docs/engine-overview.md` "빌드"), exe 실행 후 수 초 생존 확인, 결과 보고. 이 환경은 GPU 없어 WARP 폴백이 정상 | 없음 | — |
| 11 | "커밋해" | 변경 스테이징, 한국어 요약 메시지, `Co-Authored-By` 푸터. `main`이면 브랜치 먼저 | 커밋 범위·메시지 | — |

## 불변 규칙 (모든 행에 적용 — 깨지 말 것)

- D3D11 호출은 렌더 스레드에서만 (`Dx11Renderer.cpp` + `render/r2d/*` + `render/r3d/*`). 메인은 입력·시뮬·스냅샷만.
- 스레드 경계는 값 기반 `RenderSnapshot`만 (`Quad`, `Scene3D`/`MeshDraw`/`CameraView` 전부 값). 가변 게임 객체 포인터 금지.
- 렌더러 코어는 device/swapchain/RT/depth + `ShaderLibrary` 만 소유. 그리는 일은 전부 `IRenderPass` 목록.
- 패스는 셰이더를 직접 `D3DCompile` 하지 않는다. `assets/shaders/*.hlsl` + `ShaderLibrary::Get`. `.hlsl`/`.hlsli` 는 커밋되는 소스.
- **2D/3D 모듈은 서로 `#include` 금지.** `math/Math2D↔3D`, `render/r2d↔r3d`, `physics/p2d↔p3d`. 공유는 각 core만. 3D는 `ENGINE_WITH_3D`로 감싸 빌드 제외 가능하게 유지.
- 충돌은 탐지만. `CollisionWorld`는 시뮬(메인 스레드)만. 응답은 physics 밖.
- SOLID 우선: 새 타입은 `= delete` 복사 방지, 소유는 `unique_ptr`, raw는 비소유, 다형성은 인터페이스.
- 데드 코드 남기지 않기. 벤치마크 스텁은 코드에 명시.
- 서드파티는 `src/vendor/`에 소스 vendor, 벤더 타입은 그걸 쓰는 `.cpp` 안에만 (밖으로는 엔진 타입).
- **설계 문서를 만들면 "사용 방법(How to use)" 항목 필수.**
- 상세 계약: `docs/engine-overview.md`, `engine-conventions.md`, `roadmap.md`, `multithreaded_game_engine_architecture.md`, `ui-architecture.md`, `scrollable-list-and-pool.md`, `texture-atlas-and-sprite-pass.md`, `atlas-build-pipeline.md`, `scene-flow-design.md`, `game-settings.md`, `synopsis.md`, `entity-lifecycle-design.md`, `instanced-rendering.md`, `horde-design.md`, `loading-and-streaming.md`, `time-design.md`, `collider-design.md`, `model-animation-research.md`, `animation-design.md`, `audio-design.md`, `shader-pipeline.md`, `toon-rendering.md`, `lighting.md`, `msaa.md`, `shadows.md`.
